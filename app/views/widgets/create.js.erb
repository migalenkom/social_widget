$('#createNewWidget').popover("destroy");
var createdWidget = $("<%= j render "widget" , wdg:@created_widget %>")
$('.column').first().append(createdWidget);
loadWidgetContentIfVisible(createdWidget);
onFirstExpandWidget(createdWidget);
createdWidget.find(".portlet-toggle").click(function () {
    onToggleWidgetContent(this)
});

